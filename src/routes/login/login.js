import React, { useState } from 'react';
import * as Styled from './login.styled';

export const Login = () => {

    return (
        <Styled.Container>
            <Styled.NameTitle>Login</Styled.NameTitle>
        </Styled.Container>
    );
}